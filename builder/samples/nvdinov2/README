Download the deepstream SDK here https://drive.google.com/file/d/1UPJAKLrXSQOF8fM4Abu-FzqyqSnaLiWV/view?usp=sharing, and copy it to builder/samples/nvdinov2

Build the NIM inference flow:

cd builder
python main.py samples/nvdinov2/ds_nvdinov2.yaml --server-type fastapi -a samples/nvdinov2/openapi.yaml -o samples

Build and run the docker image:

cd samples
docker compose up --build nim-nvdinov2

Test the NIM with a client:

cd builder/samples/nvdinov2
python nim_client.py --host 127.0.0.1 --port 8800 --file test.jpg